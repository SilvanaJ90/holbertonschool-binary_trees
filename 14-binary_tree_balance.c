#include "binary_trees.h"

/**
 * vinary_tree_balance - check code
 * @tree: pointer to the root node of the tree to measure the balance factor
 * Return: always 0
 */

int binary_tree_balance(const binary_tree_t *tree)
{
	if (tree == NULL)
		return (0);
}
